VanillaTilt.init(document.querySelectorAll(".main-content"), {
    max: 20,
    speed: 2000,
    scale: 1.05,
    speed: 3000,
    glare: true,  // if it should have a "glare" effect
    "max-glare": 0.1,
});